---
import Layout from "../layouts/Layout.astro";
import Sep from "../components/Sep.astro";
import Service from "../components/Service.astro";
import Contact from "../components/Contact.astro";
---

<!-- 
#EDF1D6
#9DC08B
#609966
#40513B
 -->
<Layout title="DPgarden">
  <main class="relative">
    <section
      class="flex h-screen w-full flex-col items-center justify-center gap-4 bg-[url(ar1.jpg)] bg-cover bg-center text-center"
    >
      <img
        src="logo.jpg"
        alt=""
        class="aspect-square w-1/2 rounded-full drop-shadow-[0_4px_3px_rgba(0,0,0,0.5)] transition-all md:w-1/6"
      />
      <h1
        class="font-varela text-5xl font-bold text-gray-900 drop-shadow-[0_4px_3px_rgba(0,0,0,0.5)]"
      >
        עיצוב הטבע לחיים
        <span class="font-bold text-green-700">משובחים</span>
      </h1>
      <div class="flex flex-col gap-4 md:flex-row">
        <a
          href="#"
          class="mt-1 flex flex-row items-center justify-between gap-4 rounded-2xl border-b-4 border-green-800 bg-green-600 px-2 py-1 pr-4 text-center text-lg text-white transition-all hover:mt-0 hover:border-b-8 active:translate-y-1 active:border-b-4"
          >Whatsapp<i class="fa-brands fa-whatsapp"></i>
        </a>
        <a
          href="#"
          class="mt-1 flex flex-row items-center justify-between gap-4 rounded-2xl border-b-4 border-gray-400 bg-white px-2 py-1 pr-4 text-center text-lg text-black transition-all hover:mt-0 hover:border-b-8 active:translate-y-1 active:border-b-4"
          >למחשבון הצעת מחיר<i class="fa-regular fa-face-smile"></i>
        </a>
      </div>
    </section>
    <div
      class="my-12 mx-auto flex w-full justify-around gap-y-5 py-5 text-center text-xl font-medium max-sm:flex-col xl:text-2xl"
    >
      <div>
        <i
          class="fa-regular fa-heart text-5xl text-gray-700 transition-all hover:scale-110 lg:text-6xl"
        ></i>
        <p class="">אחריות מלאה</p>
      </div>
      <div>
        <i
          class="fa-regular fa-user text-5xl text-gray-700 transition-all hover:scale-110 lg:text-6xl"
        ></i>
        <p class="">צוות מנוסה וחביב</p>
      </div>
      <div>
        <i
          class="fa-regular fa-clock text-5xl text-gray-700 transition-all hover:scale-110 lg:text-6xl"
        ></i>
        <p class="">שעות עבודה ותקציב קבועות</p>
      </div>
      <div>
        <i
          class="fa-regular fa-circle-check text-5xl text-gray-700 transition-all hover:scale-110 lg:text-6xl"
        ></i>
        <p class="">תכנון ותיאום ציפיות לפרויקט</p>
      </div>
    </div>
    <section
      class="flex w-full flex-col items-center justify-center px-10 text-center"
    >
      <h1 class="font-varela text-5xl font-bold xl:text-6xl">שירותים</h1>
      <Sep />
      <h4 class="mb-2 font-heebo text-lg text-gray-500 md:text-2xl xl:text-3xl">
        אנו בונים מתחזקים ומשדרגים את גינת החלומות שלכם
      </h4>
      <p class="max-w-5xl text-lg md:text-2xl xl:text-3xl">
        אנו מתמחים בהקמת גנים ירוקים וחסרי פשרות עם תפיסה ייחודית לפרטיות
        ואסתטיקה. חברתנו מספקת מגוון שירותי הקמה, עיצוב ושיפוץ לגנים ונדל"ן. <br
        />
        כמו כן, אנו מספקים שירותי תחזוקה לגנים ונדל"ן כדי להבטיח גידול נכון וטיפול
        בסביבה הירוקה שלכם. בנוסף, אנו מתמחים בהקמת גגות ירוקים, בניית פרגולות ובמספקים
        שירותי פיתוח והקמת כניסות ובריכות שחייה לגינות שלכם.
      </p>
    </section>
    <section
      class="mt-8 flex w-full flex-col items-center justify-center px-10 text-center"
    >
      <h1 class="font-varela text-3xl font-bold xl:text-4xl">למה אנחנו</h1>
      <Sep />

      <p class="max-w-5xl text-lg md:text-2xl xl:text-3xl">
        אנחנו לא רק יוצרים גנים יפים, אנחנו מעצבים מרחבי חוץ אישיים שמשפרים את
        איכות החיים שלכם. הצוות המיומן שלנו משתמש בחומרי בניין וצמחים באיכות
        גבוהה כדי לספק תוצאות מדהימות שמחזיקות לאורך זמן.
      </p>

      <h4 class="mt-2 font-heebo text-lg font-bold md:text-2xl xl:text-3xl">
        מהרעיון ועד התחזוקה, אנחנו איתכם בכל שלב. יחדיו, נהפוך את החזון שלכם
        למציאות.
      </h4>
    </section>

    <a
      href="#"
      class="mx-auto mt-3 flex w-56 flex-row items-center justify-between gap-4 rounded-2xl border-b-4 border-gray-300 bg-gray-600 px-2 py-1 pr-4 text-center text-lg text-white transition-all hover:mt-2 hover:border-b-8 active:translate-y-1 active:border-b-4"
      >למחשבון הצעת מחיר<i class="fa-regular fa-face-smile"></i>
    </a>

    <img
      src="/ar3.jpg"
      alt=""
      class="mt-7 h-[260px] w-full object-cover object-center xl:h-[560px]"
    />

    <section class="w-full space-y-3 px-12 pb-16 pt-24 text-center">
      <h1 class="font-varela text-6xl font-bold">משדרגים לך את הגינה</h1>
      <h3>השאירו פרטים ונחזור אליכם בקרוב</h3>
      <form
        action="https://formsubmit.co/maorrachmiel@gmail.com"
        class="mx-auto space-y-8 lg:w-3/4 xl:w-1/2"
        onsubmit=""
        method="post"
      >
        <input
          type="text"
          name="name"
          id="name"
          class="focus:ring-primary-500 focus:border-primary-500 block w-full rounded-full border border-gray-300 bg-gray-50 p-2.5 text-base text-gray-900 shadow-sm"
          required
          placeholder="שם מלא"
        />

        <input
          type="number"
          name="phone"
          id="phone"
          class="focus:ring-primary-500 focus:border-primary-500 block w-full rounded-full border border-gray-300 bg-gray-50 p-2.5 text-base text-gray-900 shadow-sm"
          required
          placeholder="מספר טלפון"
        />

        <textarea
          id="message"
          name="message"
          rows="6"
          class="focus:ring-primary-500 focus:border-primary-500 block w-full rounded-3xl border border-gray-300 bg-gray-50 p-2.5 text-base text-gray-900 shadow-sm"
          style="direction:RTL;"
          placeholder="הודעה (לא חובה)"></textarea>

        <button
          type="submit"
          class="w-full rounded-full bg-gray-900 py-2 text-center font-heebo text-xl font-semibold text-white shadow-sm transition-all duration-150 hover:scale-105 hover:shadow-md focus:outline-none focus:ring-4"
          >חזור אליי!</button
        >
      </form>
    </section>

    <section
      class="flex h-screen w-full flex-col items-center justify-center bg-gray-900 text-white"
    >
      <h1>lorem</h1>
      <h3>ipsum</h3>
      <form id="regForm">
        <h1>Register:</h1>
        <!-- One "tab" for each step in the form: -->
        <div class="tab" style="display: none;">
          Name:
          <p>
            <input placeholder="First name..." name="fname" />
          </p>
          <p>
            <input placeholder="Last name..." name="lname" />
          </p>
        </div>
        <div class="tab" style="display: none;">
          Contact Info:
          <p>
            <input placeholder="E-mail..." name="email" />
          </p>
          <p>
            <input placeholder="Phone..." name="phone" />
          </p>
        </div>
        <div class="tab" style="display: none;">
          Birthday:
          <p>
            <input placeholder="dd" name="dd" />
          </p>
          <p>
            <input placeholder="mm" name="nn" />
          </p>
          <p>
            <input placeholder="yyyy" name="yyyy" />
          </p>
        </div>
        <div class="tab" style="display: none;">
          Login Info:
          <p>
            <input placeholder="Username..." name="uname" />
          </p>
          <p>
            <input placeholder="Password..." name="pword" type="password" />
          </p>
        </div>
        <div style="overflow:auto;">
          <div style="float:right;">
            <button type="button" id="prevBtn" onclick="nextPrev(-1)"
              >Previous</button
            >
            <button type="button" id="nextBtn" onclick="nextPrev(1)"
              >Next</button
            >
          </div>
        </div>
        <!-- Circles which indicates the steps of the form: -->
        <div style="text-align:center;margin-top:40px;">
          <span class="step"></span>
          <span class="step"></span>
          <span class="step"></span>
          <span class="step"></span>
        </div>
      </form>
    </section>
  </main>
  <!-- step form -->
  <script is:inline>
    var currentTab = 0; // Current tab is set to be the first tab (0)
    showTab(currentTab); // Display the current tab

    function showTab(n) {
      // This function will display the specified tab of the form ...
      var x = document.getElementsByClassName("tab");
      x[n].style.display = "block";
      // ... and fix the Previous/Next buttons:
      if (n == 0) {
        document.getElementById("prevBtn").style.display = "none";
      } else {
        document.getElementById("prevBtn").style.display = "inline";
      }
      if (n == x.length - 1) {
        document.getElementById("nextBtn").innerHTML = "Submit";
      } else {
        document.getElementById("nextBtn").innerHTML = "Next";
      }
      // ... and run a function that displays the correct step indicator:
      fixStepIndicator(n);
    }

    function nextPrev(n) {
      // This function will figure out which tab to display
      var x = document.getElementsByClassName("tab");
      // Exit the function if any field in the current tab is invalid:
      if (n == 1 && !validateForm()) return false;
      // Hide the current tab:
      x[currentTab].style.display = "none";
      // Increase or decrease the current tab by 1:
      currentTab = currentTab + n;
      // if you have reached the end of the form... :
      if (currentTab >= x.length) {
        //...the form gets submitted:
        document.getElementById("regForm").submit();
        return false;
      }
      // Otherwise, display the correct tab:
      showTab(currentTab);
    }

    function validateForm() {
      // This function deals with validation of the form fields
      var x,
        y,
        i,
        valid = true;
      x = document.getElementsByClassName("tab");
      y = x[currentTab].getElementsByTagName("input");
      // A loop that checks every input field in the current tab:
      for (i = 0; i < y.length; i++) {
        // If a field is empty...
        if (y[i].value == "" && y[i].required == true) {
          // add an "invalid" class to the field:
          y[i].className += " invalid";
          // and set the current valid status to false:
          valid = false;
        }
      }
      // If the valid status is true, mark the step as finished and valid:
      if (valid) {
        document.getElementsByClassName("step")[currentTab].className +=
          " finish";
      }
      return valid; // return the valid status
    }

    function fixStepIndicator(n) {
      // This function removes the "active" class of all steps...
      var i,
        x = document.getElementsByClassName("step");
      for (i = 0; i < x.length; i++) {
        x[i].className = x[i].className.replace(" active", "");
      }
      //... and adds the "active" class to the current step:
      x[n].className += " active";
    }
  </script>
  <!-- accesibility -->
  <script>
    const callback = function (entries: IntersectionObserverEntry[]) {
      entries.forEach((entry: IntersectionObserverEntry) => {
        console.log(entry);

        if (entry.isIntersecting) {
          entry.target.classList.add("animate-fadeIn");
        } else {
          entry.target.classList.remove("animate-fadeIn");
        }
      });
    };
    const observer = new IntersectionObserver(callback);

    const targets = document.querySelectorAll(".js-show-on-scroll");
    targets.forEach(function (target) {
      target.classList.add("opacity-0");
      observer.observe(target);
    });
  </script>
</Layout>
